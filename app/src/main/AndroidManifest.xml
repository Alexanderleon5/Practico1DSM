<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sv.edu.udb.practico1dsm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practico1DSM">

        <!-- Actividad Principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true"> <!-- Agrega android:exported="true" -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad para calcular el promedio -->
        <activity
            android:name=".PromedioActivity"
            android:exported="false" /> <!-- No es la actividad principal, no necesita intent-filter -->

        <!-- Actividad para calcular el salario -->
        <activity
            android:name=".DescuentosActivity"
            android:exported="false" /> <!-- No es la actividad principal, no necesita intent-filter -->

        <!-- Actividad para la calculadora -->
        <activity
            android:name=".CalculadoraActivity"
            android:exported="false" /> <!-- No es la actividad principal, no necesita intent-filter -->

    </application>

</manifest>